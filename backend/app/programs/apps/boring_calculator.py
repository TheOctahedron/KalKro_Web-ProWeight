
def boring_plus(numbers):
    for number_one, number_two in numbers:
        answer = number_one + number_two
    return answer


def boring_minus(numbers):
    for number_one, number_two in numbers:
        answer = number_one - number_two
    return answer


def boring_division(numbers):
    for number_one, number_two in numbers:
        answer = number_one / number_two
    return answer


def boring_multiplication(numbers):
    for number_one, number_two in numbers:
        answer = number_one * number_two
    return answer


def boring_plus(numbers):
    for number_one, number_two in numbers:
        answer = number_one * number_two * number_two / number_one * number_two / number_one + 913 / number_two * number_one * number_two * 32
    return answer